# DO NOT EDIT THIS FILE MANUALLY
# This file is generated from a template located at:
# terraform/modules/aws/bastion/templates/inventory.tmpl

[kube_control_plane]
%{ if length(trimspace(control_hosts)) > 0 }
${control_hosts}
%{ else }
# (no control hosts)
%{ endif }

[kube_worker_nodes]
%{ if length(trimspace(worker_hosts)) > 0 }
${worker_hosts}
%{ else }
# (no worker hosts)
%{ endif }

[haproxy_nodes]
%{ if length(trimspace(haproxy_hosts)) > 0 }
${haproxy_hosts}
%{ else }
# (no haproxy hosts)
%{ endif }

[all:vars]
ansible_user=${ansible_user}
ansible_ssh_private_key_file=${ansible_key}
web_server_ip_1=192.168.1.10
web_server_ip_2=192.168.1.11
haproxy_stats_user=stats
haproxy_stats_password=${haproxy_stats_password}
# k8s_net should match calico-custom-resources.yaml
k8s_net=10.64.0.0/16
