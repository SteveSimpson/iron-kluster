# DO NOT EDIT THIS FILE MANUALLY
# This file is generated from a template located at:
# terraform/modules/aws/bastion/templates/inventory.tmpl

[kube_control_plane]
%{ if length(trimspace(control_hosts)) > 0 }
${control_hosts}
%{ else }
# (no control hosts)
%{ endif }

[kube_worker_nodes]
%{ if length(trimspace(worker_hosts)) > 0 }
${worker_hosts}
%{ else }
# (no worker hosts)
%{ endif }

[all:vars]
ansible_user=${ansible_user}
ansible_ssh_private_key_file=${ansible_key}
